#include "main.h"
#include <stdio.h>

/**
 * mul -multiplies inter
 * @a: - the first inter
 * @b: -second inte
 * Return: always 0
 */

int mul(int a, int b)
{
int result = a * b;
return ( result);
}
